name: Keep Render Alive

on:
  schedule:
    - cron: "*/14 * * * *"   # Every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend health
        run: |
          curl -sf "${{ secrets.RENDER_HEALTH_URL }}/api/health" || echo "Backend not responding"
